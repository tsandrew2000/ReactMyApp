import React from "react"

const Header = () => {
  return (
    <header>
      <h1>Add Classes Application</h1>
      <p>Add classes using the input box below</p>
    </header>
  )
}

export default Header